name: Public flow probe

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - agent/full-release-hardening-20260724
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install reproducibly
        run: npm ci --no-audit --no-fund

      - name: Install Chromium probe tools
        run: |
          npm install --no-save --no-package-lock playwright
          npx playwright install --with-deps chromium

      - name: Probe public production without signing
        run: node scripts/preview-flow-probe.mjs
        env:
          AUDIT_BASE_URL: https://ott-terminal-mvp.vercel.app
          AUDIT_OUTPUT_DIR: artifacts/public-flow-probe

      - name: Upload probe evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ott-public-flow-probe
          path: artifacts/public-flow-probe
          if-no-files-found: error
          retention-days: 14
