name: Live Production Smoke Test

on:
  push:
    branches:
      - qa/live-production-smoke
  workflow_dispatch:

permissions:
  contents: read

jobs:
  live-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Checkout QA branch
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Playwright
        run: npm install --no-save playwright@1.54.1

      - name: Install Chromium dependencies
        run: npx playwright install --with-deps chromium

      - name: Run live OTT Terminal smoke test
        env:
          BASE_URL: https://ott-terminal-mvp.vercel.app
        run: node scripts/live-smoke-test.mjs

      - name: Upload smoke test report and screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ott-live-smoke-report
          path: artifacts/live-smoke
          if-no-files-found: error
          retention-days: 14
